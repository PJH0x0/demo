public class Solution {
    public String simplfyPath(String path) {
        if ("".equals(path)) return 
        char[] stack = new char[path.length()];
        int point = 0;
        char symbol = path.charAt(0);
        for(int i = 0; i < path.length(); i++) {
            symbol = path.charAt(i);
            switch
        }
    }
}
